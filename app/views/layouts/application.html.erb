<!DOCTYPE html>
<html>
<head>
	<%# title provided on each page for prefix here %>
  <title><%=full_title(yield(:title))%></title>
  <meta name="description" content="Traveller's Log Book - The colloborative travel guide, 'Log' your experiences or be a part of someone else's adventure.">
  <meta http-equiv="content-type" content="text/html;charset=UTF-8">
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%# bootstrap %>
  <link rel="stylesheet" href="//maxcdn.bootstrapcdn.com/bootstrap/3.3.5/css/bootstrap.min.css">
  <script src="//maxcdn.bootstrapcdn.com/bootstrap/3.3.5/js/bootstrap.min.js"></script>
  <%# google maps api %>
  <script src="//maps.googleapis.com/maps/api/js"></script>
  <%# prevents CSRF attack %>
    <%= csrf_meta_tags %>
    <%# font for title page %>
    <link href='//fonts.googleapis.com/css?family=Kaushan+Script' rel='stylesheet' type='text/css'>
</head>
<body>
	<%= render 'layouts/header' %>
<div class='container top-margin'>
	<div class='row' >
		<div class='col-xs-2 side-panel side-panel-left'>
          <% flash.each do |message_type, message| %>
        <div class="alert alert-<%= message_type %>"><%= message %></div>
      <% end %>

    <%= render 'layouts/sidepanel' %>
		</div>
		<div class='col-xs-10'>
			<%= yield %>
      <%= debug(params) if Rails.env.development? %>
		</div>
</div>
</div>
<%= render 'layouts/footer' %>

</body>
</html>
